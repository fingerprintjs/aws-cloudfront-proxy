export const agentV4Paths = {
  ingress: '/info',
  cdn: '/web(/.*)?',
  browserCache: '/help',
}

export const CDN_PATH = '/web(/.*)?'

// In V4, the ingress path is just /
export const INGRESS_PATH = '/.*'
